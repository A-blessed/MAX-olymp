[pytest]
# Тесты импортируют пакет app, поэтому корень backend/ должен быть на sys.path.
pythonpath = .
testpaths = tests
# Асинхронные тесты не нужно помечать декоратором вручную.
asyncio_mode = auto
addopts = -q
