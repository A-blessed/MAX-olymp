# Зависимости времени выполнения. Версии зафиксированы точно, чтобы
# сборка была воспроизводимой. Инструменты разработки — в requirements-dev.txt.

fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# HTTP-клиент для Bot API MAX.
httpx==0.28.1
# Набор корневых сертификатов, к которому добавляются сертификаты Минцифры.
certifi==2026.7.22

SQLAlchemy==2.0.54
# Миграции схемы: create_all умеет только создавать недостающие таблицы.
alembic==1.20.0
asyncpg==0.31.0
# Требуется асинхронному слою SQLAlchemy.
greenlet==3.5.6
